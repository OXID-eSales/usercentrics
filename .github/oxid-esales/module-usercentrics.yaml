install:
  cache:
    prepared_shop: false
  git:
    repository: 'OXID-eSales/usercentrics'
    ref: &ref '{{ .Github.RefName }}'
    shop_url: 'https://github.com/OXID-eSales/usercentrics.git'
    shop_ref: *ref
  composer:
    root_url: ''
    transform: |
      {
          "require": {
              "oxid-esales/oxideshop-ce": "{{ .Data.global.composer.dev_ref }}",
              "oxid-esales/twig-admin-theme": "{{ .Data.global.composer.dev_ref }}",
              "oxid-esales/apex-theme": "{{ .Data.global.composer.dev_ref }}",
              "oxid-esales/twig-component": "{{ .Data.global.composer.dev_ref }}"
          }
      }
  custom_script_container: |
    vendor/bin/oe-console oe:database:reset --db-host=mysql --db-port=3306 --db-name=example --db-user=root --db-password=root --force
    vendor/bin/oe-console oe:module:install ./
    vendor/bin/oe-eshop-doctrine_migration migrations:migrate
    vendor/bin/oe-console oe:module:activate oxps_usercentrics
    vendor/bin/oe-console oe:theme:activate apex

install_shop_with_modules:
  composer:
    root_url: ''

runscript: &runscript
  matrix:
    script: |
      [
        "oxps_usercentrics:tests-unit",
        "oxps_usercentrics:tests-integration",
        "oxps_usercentrics:tests-codeception"
      ]
  oxps_usercentrics:
    path: ''

runslim:
  <<: *runscript
  matrix:
    script: |
      [
        "oxps_usercentrics:phpcs-report",
        "oxps_usercentrics:phpmd-report",
        "oxps_usercentrics:phpstan-report"
      ]

sonarcloud:
  matrix:
    testplan: '["-"]'
  strip_path: '/var/www/'
  project_key: 'OXID-eSales_usercentrics'
  project_name: 'oxid-professional-services/usercentrics'
  organization: 'oxid-esales'
  parameters: |
    -Dsonar.language=php
    -Dsonar.scm.provider=git
    -Dsonar.sources=src
    -Dsonar.tests=tests
    -Dsonar.php.phpstan.reportPaths=coverage-reports/phpstan.report.json

finish:
  slack_title: 'Module oxps_usercentrics ({{ .Data.global.git.shop_ref }}) by {{ .Github.Actor }}'