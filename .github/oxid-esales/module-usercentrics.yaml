install:
  cache:
    prepared_shop: false
  composer:
    root_url: ''
    transform: |
      {
        "require": {
          "oxid-esales/oxideshop-ce": "v7.4.0",
          "oxid-esales/twig-admin-theme": "v3.0.0",
          "oxid-esales/twig-component": "v2.7.0",
          "oxid-esales/apex-theme": "v3.0.0"
        }
      }
  custom_script_container: |
    vendor/bin/oe-console oe:database:reset --db-host=mysql --db-port=3306 --db-name=example --db-user=root --db-password=root --force
    vendor/bin/oe-console oe:module:install ./
    
    vendor/bin/oe-eshop-doctrine_migration migrations:migrate
    
    vendor/bin/oe-console oe:module:activate oxps_usercentrics
    vendor/bin/oe-console oe:theme:activate apex

runscript: &runscript
  matrix:
    script: |
      [
        "report:tests-unit",
        "report:tests-integration",
        "no_report:tests-codeception",
      ]
  report:
    path: ''
  no_report:
    path: ''
    coverage_prefix: ''

runslim:
  <<: *runscript
  matrix:
    script: |
      [
        "report:phpstan-report",
        "no_report:phpmd-report",
        "no_report:phpcs-report",
      ]

sonarcloud:
  matrix:
    testplan: '["-"]'
  strip_path: '/var/www/'
  organization: 'oxid-esales'
  project_name: 'oxid-professional-services/usercentrics'
  project_key: 'OXID-eSales_usercentrics'
  target_branch: 'b-7.4.x'
  parameters: >
    -Dsonar.language=php
    -Dsonar.scm.provider=git
    -Dsonar.sources=src
    -Dsonar.tests=tests

finish:
  slack_title: 'Module UserCentrics ({{ .Data.install.git.ref }}) by {{ .Github.Actor }}'